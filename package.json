{
  "name": "landscapejs",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "vite build && node updateVersion.js && ./publish.sh"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  }
}